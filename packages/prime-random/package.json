{
  "name": "@prime/prime-random",
  "version": "0.1.0",
  "description": "Seeded RNG — deterministic cross-platform randomness",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
