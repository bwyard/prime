{
  "name": "@prime/prime-render",
  "version": "0.1.0",
  "description": "Pure sample-level scan loop — fold a pure step function over N samples",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
