{
  "name": "@prime/prime-noise",
  "version": "0.1.0",
  "description": "TypeScript bindings for prime-noise (WASM wrapper)",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
